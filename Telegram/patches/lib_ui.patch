diff --git a/ui/style/style_core_icon.cpp b/ui/style/style_core_icon.cpp
index 0886094..04a5820 100644
--- a/ui/style/style_core_icon.cpp
+++ b/ui/style/style_core_icon.cpp
@@ -32,11 +32,19 @@ QMutex IconMasksMutex;
 base::flat_map<std::pair<const IconMask*, uint32>, QPixmap> iconPixmaps;
 base::flat_set<IconData*> iconData;
 
+IconMaskOverrideHook GlobalIconMaskOverrideHook = nullptr;
+
 [[nodiscard]] QImage CreateIconMask(
 		not_null<const IconMask*> mask,
 		int scale,
 		bool ignoreDpr = false) {
 	const auto ratio = ignoreDpr ? 1 : DevicePixelRatio();
+	if (GlobalIconMaskOverrideHook) {
+		auto custom = GlobalIconMaskOverrideHook(mask, scale, ratio);
+		if (!custom.isNull()) {
+			return custom;
+		}
+	}
 	const auto realscale = scale * ratio;
 
 	auto data = QByteArray::fromRawData(
@@ -160,6 +168,10 @@ QSize IconData::size(int scale) const {
 	return QSize();
 }
 
 } // namespace
+
+void SetIconMaskOverrideHook(IconMaskOverrideHook hook) {
+	GlobalIconMaskOverrideHook = hook;
+}
 
 MonoIcon::MonoIcon(const MonoIcon &other, const style::palette &palette)
@@ -178,6 +190,8 @@ MonoIcon::MonoIcon(const IconMask *mask, Color color, QMargins padding)
 
 void MonoIcon::reset() const {
 	_pixmap = QPixmap();
+	_maskImage = QImage();
+	_colorizedImage = QImage();
 	_size = QSize();
 }
 
@@ -510,6 +524,9 @@ void ResetIcons() {
 	for (const auto data : iconData) {
 		data->reset();
 	}
+
+	QMutexLocker lock(&IconMasksMutex);
+	IconMasks.clear();
 }
 
 void DestroyIcons() {
diff --git a/ui/style/style_core_icon.h b/ui/style/style_core_icon.h
index f02f227..76e5c8d 100644
--- a/ui/style/style_core_icon.h
+++ b/ui/style/style_core_icon.h
@@ -88,6 +88,10 @@ public:
 		int scale,
 		bool ignoreDpr) const;
 
+	[[nodiscard]] const IconMask *mask() const {
+		return _mask;
+	}
+
 	~MonoIcon() {
 	}
 
@@ -126,6 +130,9 @@ public:
 	bool empty() const {
 		return _parts.empty();
 	}
+	[[nodiscard]] const std::vector<MonoIcon> &parts() const {
+		return _parts;
+	}
 
 	void paint(QPainter &p, const QPoint &pos, int outerw) const {
 		for (const auto &part : _parts) {
@@ -225,6 +232,9 @@ public:
 	bool empty() const {
 		return _data->empty();
 	}
+	[[nodiscard]] const IconMask *mask() const {
+		return (_data && !_data->empty() && !_data->parts().empty()) ? _data->parts()[0].mask() : nullptr;
+	}
 	void reset() const {
 		if (_data) {
 			_data->reset();
@@ -392,5 +402,8 @@ private:
 void ResetIcons();
 void DestroyIcons();
 
+using IconMaskOverrideHook = QImage (*)(const IconMask *mask, int scale, int ratio);
+void SetIconMaskOverrideHook(IconMaskOverrideHook hook);
+
 } // namespace internal
 } // namespace style
diff --git a/ui/text/text_entity.cpp b/ui/text/text_entity.cpp
index aa9c1b3..029346d 100644
--- a/ui/text/text_entity.cpp
+++ b/ui/text/text_entity.cpp
@@ -2350,6 +2350,14 @@ EntitiesInText ConvertTextTagsToEntities(const TextWithTags::Tags &tags) {
 				result.collapsed = 1;
 			} else if (single == Tags::kTagSpoiler) {
 				result.set(EntityType::Spoiler);
+			} else if (single == Tags::kTagMargyDim) {
+				result.set(EntityType::Italic);
+			} else if (single == Tags::kTagMargyOutline) {
+				result.set(EntityType::Bold);
+			} else if (single == Tags::kTagMargyRainbow) {
+				result.set(EntityType::Underline);
+			} else if (single == Tags::kTagMargySize) {
+				result.set(EntityType::Bold);
 			} else if (IsRichFormattingTag(single)) {
 				if (single == Tags::kTagIvMarked) {
 					result.set(EntityType::Marked);
diff --git a/ui/widgets/checkbox.cpp b/ui/widgets/checkbox.cpp
index df0dc58..66e5111 100644
--- a/ui/widgets/checkbox.cpp
+++ b/ui/widgets/checkbox.cpp
@@ -84,37 +84,46 @@ void ToggleView::setStyle(const style::Toggle &st) {
 }
 
 void ToggleView::paint(QPainter &p, int left, int top, int outerWidth) {
-	left += _st->border;
-	top += _st->border;
-
 	PainterHighQualityEnabler hq(p);
-	auto toggled = currentAnimationValue();
-	auto fullWidth = _st->diameter + _st->width;
-	auto innerDiameter = _st->diameter - 2 * _st->shift;
-	auto innerRadius = float64(innerDiameter) / 2.;
-	auto toggleLeft = left + anim::interpolate(0, fullWidth - _st->diameter, toggled);
-	auto bgRect = style::rtlrect(left + _st->shift, top + _st->shift, fullWidth - 2 * _st->shift, innerDiameter, outerWidth);
-	auto fgRect = style::rtlrect(toggleLeft, top, _st->diameter, _st->diameter, outerWidth);
-	auto fgBrush = anim::brush(_st->untoggledFg, _st->toggledFg, toggled);
-
+	const auto toggled = currentAnimationValue();
+	const auto trackW = float64(2 * _st->border + _st->diameter + _st->width);
+	const auto trackH = float64(2 * _st->border + _st->diameter);
+
+	const auto trackMargin = 1.0;
+	const auto trackRect = QRectF(left + trackMargin, top + trackMargin, trackW - 2.0 * trackMargin, trackH - 2.0 * trackMargin);
+	const auto trackRadius = trackRect.height() / 2.0;
+
+	auto trackPen = QPen(anim::brush(_st->untoggledFg, _st->toggledFg, toggled), 2.0);
+	p.setPen(trackPen);
+	p.setBrush(anim::brush(_st->untoggledBg, _st->toggledFg, toggled));
+	p.drawRoundedRect(style::rtlrect(trackRect, outerWidth), trackRadius, trackRadius);
+
+	const auto H = trackRect.height();
+	const auto dOff = std::clamp(H * 0.45, 8.0, H - 6.0);
+	const auto dOn = std::clamp(H * 0.75, dOff + 2.0, H - 3.0);
+	const auto thumbDiameter = anim::interpolate(dOff, dOn, toggled);
+
+	const auto padOff = (H - dOff) / 2.0;
+	const auto padOn = (H - dOn) / 2.0;
+	const auto minX = trackRect.left() + padOff;
+	const auto maxX = trackRect.right() - padOn - dOn;
+	const auto thumbLeft = anim::interpolate(minX, maxX, toggled);
+	const auto thumbTop = trackRect.top() + (H - thumbDiameter) / 2.0;
+	const auto thumbRect = QRectF(thumbLeft, thumbTop, thumbDiameter, thumbDiameter);
+
+	const auto thumbBrush = anim::brush(_st->untoggledFg, _st->toggledBg, toggled);
 	p.setPen(Qt::NoPen);
-	p.setBrush(fgBrush);
-	p.drawRoundedRect(bgRect, innerRadius, innerRadius);
-
-	auto pen = anim::pen(_st->untoggledFg, _st->toggledFg, toggled);
-	pen.setWidth(_st->border);
-	p.setPen(pen);
-	p.setBrush(anim::brush(_st->untoggledBg, _st->toggledBg, toggled));
-	p.drawEllipse(fgRect);
+	p.setBrush(thumbBrush);
+	p.drawEllipse(style::rtlrect(thumbRect, outerWidth));
 
 	if (_locked || _st->xsize > 0) {
 		p.setPen(Qt::NoPen);
-		p.setBrush(fgBrush);
+		p.setBrush(thumbBrush);
 		if (_locked) {
 			const auto color = anim::color(_st->untoggledFg, _st->toggledFg, toggled);
-			_st->lockIcon.paint(p, toggleLeft, top, outerWidth, color);
+			_st->lockIcon.paint(p, int(thumbLeft), int(thumbTop), outerWidth, color);
 		} else {
-			paintXV(p, toggleLeft, top, outerWidth, toggled, fgBrush);
+			paintXV(p, int(thumbLeft), int(thumbTop), outerWidth, toggled, thumbBrush);
 		}
 	}
 }
diff --git a/ui/widgets/fields/input_field.cpp b/ui/widgets/fields/input_field.cpp
index e41f8df..d2de028 100644
--- a/ui/widgets/fields/input_field.cpp
+++ b/ui/widgets/fields/input_field.cpp
@@ -74,6 +74,10 @@ const auto &kTagPre = InputField::kTagPre;
 const auto &kTagBlockquote = InputField::kTagBlockquote;
 const auto &kTagBlockquoteCollapsed = InputField::kTagBlockquoteCollapsed;
 const auto &kTagSpoiler = InputField::kTagSpoiler;
+const auto &kTagMargyDim = InputField::kTagMargyDim;
+const auto &kTagMargyOutline = InputField::kTagMargyOutline;
+const auto &kTagMargyRainbow = InputField::kTagMargyRainbow;
+const auto &kTagMargySize = InputField::kTagMargySize;
 const auto kTagCheckLinkMeta = u"^:/:/:^"_q;
 const auto kSoftLine = QChar::LineSeparator;
 const auto kHardLine = QChar::ParagraphSeparator;
@@ -1084,6 +1088,17 @@ QTextImageFormat PrepareEmojiFormat(EmojiPtr emoji, style::font font) {
 		} else if (tag == kTagCode || IsTagPre(tag)) {
 			color = st::defaultTextPalette.monoFg->c;
 			font = font->monospace();
+		} else if (tag == kTagMargyDim) {
+			font = font->italic();
+			color = st.textFg->c;
+			color->setAlpha(140);
+		} else if (tag == kTagMargyOutline) {
+			font = font->bold();
+		} else if (tag == kTagMargyRainbow) {
+			font = font->underline();
+			color = QColor(255, 80, 180);
+		} else if (tag == kTagMargySize) {
+			font = font->bold();
 		} else if (instantViewEditorTagsEnabled) {
 			if (tag == InputField::kTagIvMarked) {
 				bg = st.textMarkBg->c;
@@ -1572,6 +1587,10 @@ const QString InputField::kTagIvMarked = u"iv-marked"_q;
 const QString InputField::kTagIvSubscript = u"iv-subscript"_q;
 const QString InputField::kTagIvSuperscript = u"iv-superscript"_q;
 const QString InputField::kTagIvMath = u"iv-math"_q;
+const QString InputField::kTagMargyDim = u"margy_dim"_q;
+const QString InputField::kTagMargyOutline = u"margy_outline"_q;
+const QString InputField::kTagMargyRainbow = u"margy_rainbow"_q;
+const QString InputField::kTagMargySize = u"margy_size"_q;
 const QString InputField::kCustomEmojiTagStart = u"custom-emoji://"_q;
 const QString InputField::kCustomDateTagStart = u"custom-date://"_q;
 const int InputField::kCollapsedQuoteFormat = ::Ui::kCollapsedQuoteFormat;
diff --git a/ui/widgets/fields/input_field.h b/ui/widgets/fields/input_field.h
index b97e5a9..7c3ee7d 100644
--- a/ui/widgets/fields/input_field.h
+++ b/ui/widgets/fields/input_field.h
@@ -167,6 +167,10 @@ public:
 	static const QString kTagIvSubscript;
 	static const QString kTagIvSuperscript;
 	static const QString kTagIvMath;
+	static const QString kTagMargyDim;
+	static const QString kTagMargyOutline;
+	static const QString kTagMargyRainbow;
+	static const QString kTagMargySize;
 	static const QString kCustomEmojiTagStart;
 	static const QString kCustomDateTagStart;
 	static const int kCollapsedQuoteFormat; // QTextFormat::ObjectTypes
